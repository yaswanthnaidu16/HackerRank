#Find the maximum possible sum of any contiguous subarray in the given array

class Solution:
    def maxSubarraySum(self, arr):
        curr = arr[0]
        ans = arr[0]

        for i in range(1, len(arr)):
            curr = max(arr[i], curr + arr[i])
            ans = max(ans, curr)

        return ans
